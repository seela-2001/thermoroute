apiVersion: v1
kind: Service
metadata:
  name: thermoroute-postgres
  namespace: {{ .Values.namespace }}

spec:
  type: ClusterIP

  selector:
    app: thermoroute-postgres

  ports:
    - port: 5432
      targetPort: 5432
